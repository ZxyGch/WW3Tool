# WW3 嵌套网格设计与问题分析报告

本报告基于 WW3 源码(`/Users/zxy/ocean/Paper/WW3`)、官方手册(`manual/app/nest.tex`、`manual/num/nest.tex`)与 WW3Tool 当前实现,梳理 WW3 嵌套网格的正确做法,指出 WW3Tool 现有设计的问题,并给出改进方案。

## 1. WW3 提供的两种嵌套机制

WW3 有两条彼此独立的嵌套路线,适用场景不同,**不可混用**。

### 1.1 单向嵌套(传统方式:`ww3_bound` / `ww3_shel` + 边界谱)

依据 `manual/app/nest.tex`:

- 大尺度(低分辨率)模型先运行,在其网格上指定一批**输出边界点**,运行后产出一个边界谱文件(如 `nest1.ww3`)。
- 小尺度(高分辨率)模型把同一批点设为**输入边界点**,运行时自动读取 `nest.ww3`,在边界上更新二维谱。
- 二者**时间上解耦、顺序执行**:粗网格 → 边界谱 → 细网格。能量只从粗 → 细单向传递,细网格不反馈给粗网格。
- 也可用 `ww3_bound` 从外部/第三方数据库(如全球再分析)直接生成边界谱,无需自己跑粗网格。

特点:**实现简单、健壮、可控**;两网格的谱、网格、bathymetry 可完全独立;唯一约束是边界点位置一致、谱离散一致。

### 1.2 双向 嵌套 / 马赛克多网格(`ww3_multi`)

依据 `manual/num/nest.tex` §two-way 与 `ww3/ww3_multi.nml`:

- 一次性定义**多个任意分辨率的网格**,在每个相关时间步**自动交换数据**,由 `ww3_multi` 统一驱动。
- 每个网格有 **RANK(等级)**:`MODEL(I)%RESOURCE%RANK_ID`,**rank 越低分辨率越低**(粗网格 rank 小,细网格 rank 大)。
- 三类数据传递:① 低 rank → 高 rank(给细网格提供边界,等价于单向嵌套);② **高 rank → 低 rank(反馈,这才构成双向)**;③ 同 rank 之间。
- 高 → 低 的反馈在"高 rank 网格在时间上追上低 rank 网格后",用**能量守恒权重**把细网格能量平均回粗网格(手册给出 $E_{l,i}=\sum w_{ij}E_{h,j}$)。
- 顶层 `DOMAIN_NML` 的 **`FLGHG1`/`FLGHG2`** 控制双向嵌套时粗网格在细网格覆盖区的**计算掩码**(`FLGHG1`)与**输出掩码**(`FLGHG2`)。
- `RESOURCE` 行 `RANK GROUP COMM_FRAC_lo COMM_FRAC_hi BOUND_FLAG`:`GROUP` 控制并发(同组串行、异组可并发),`COMM_FRAC` 把 MPI 进程按区间切给各网格,`BOUND_FLAG` 控制是否为该网格 dump 边界数据。

特点:**真正的两网格交互、可双向**;但要求网格**重叠正确、边界落在对方有效(湿)网格上、谱离散一致**,资源切分(`COMM_FRAC`)直接决定负载均衡。



## 2. WW3Tool 当前实现

| 环节 | 现状 | 代码位置 |
| --- | --- | --- |
| 网格生成 | `grid_type=nested` 时,在 `coarse/`、`fine/` 两个子目录**各自独立**跑 pygridgen,分别用 `outer`、`inner` 区域 | `grid_generation_adapter.py:_generate_structured` |
| inner 区域 | 由 `outer` 按 `nested_contraction_coefficient`(默认 1.3)**机械收缩**得到,或显式 `structured.nested.inner` | `configuration.py:_contract_region` |
| 运行驱动 | 用 **`ww3_multi`**(马赛克路线),`ww3_multi.nml` 模板含 coarse(RANK 1)+ fine(RANK 2) | `local.sh`/`server.sh`、`ww3_multi_nml.py` |
| 资源切分 | `COMM_FRAC` **按网格点数 nx·ny 动态计算**(粗网格保底 0.35、+5% 权重、封顶 0.60) | `ww3_multi_nml.py:110-150` |
| 边界/双向 | `BOUND_FLAG = F`;**`FLGHG1`/`FLGHG2` 完全未设置**(沿用模板默认 F) | `ww3_multi_nml.py:420-435` |
| 谱离散 | 内外圈**共享同一套谱**(同一 `SPECTRUM%` 参数)| —(正确) |
| 时间步 | 提供 `inner_compute_precision`/`inner_output_precision` 区分内圈步长 | `config_models.py` |

## 3. 存在的问题

### 3.1 双向嵌套的关键开关 `FLGHG1`/`FLGHG2` 从未配置(最核心问题)

WW3Tool 走的是 `ww3_multi` 马赛克路线,但**两向嵌套的掩码标志 `FLGHG1`/`FLGHG2` 一直是模板默认值 `F`**。后果:

- 粗网格在细网格覆盖区域**仍然全量计算**(`FLGHG1=F` 不掩码),纯属浪费算力;
- "是单向还是双向"在配置上**语义不明**——既没明确关掉反馈做干净的单向,也没正确开启掩码做规范的双向。


### 3.2 两个网格独立生成 → 嵌套边界不兼容
`coarse/` 与 `fine/` 各自独立跑 pygridgen,**各自的 bathymetry、coastline、land-sea mask**。而 `ww3_multi` 双向嵌套要求:细网格的边界点必须落在粗网格的**有效(湿)网格**上,且两网格在重叠区水深/掩码自洽。独立生成**不保证**:
- 细网格边界处在粗网格里可能恰好是**陆地/掩码点** → 取不到边界谱、嵌套交换报错或得到零能量;
- 重叠区水深不一致 → 能量反馈(高→低)出现伪梯度。

### 3.3 `inner` 用"机械收缩"定义,不符合嵌套的物理意图
真正的嵌套高分辨率区应是用户按**感兴趣区域(如上海近岸、某海湾)**指定的局部 patch。WW3Tool 默认把 `outer` 按 1.3 等比收缩、**中心对齐**地缩小成 inner——产出的 inner 既不一定覆盖关心的近岸,也只比 outer 小一圈(默认仅缩 ~23%),失去"小区域高分辨率"的意义。

### 3.4 `COMM_FRAC` 仅按网格点数分配,忽略 CFL 时间步
细网格分辨率更高,受 CFL 限制**时间步更小、单位时间内步数更多**,每个网格点的总计算量远高于粗网格。仅按 `nx·ny` 点数分进程,会**系统性低估细网格的真实计算量** → 细网格分到的进程偏少、粗网格进程空等,负载不均、墙钟时间被细网格拖长。应按 **点数 ×(1/Δt)** 或实测耗时加权。

### 3.5 缺少更稳健的单向嵌套(`ww3_bound`)路径
对"只想在小区域得到高分辨率结果、不需要反馈"的常见需求,**单向嵌套(§1.1)更简单健壮**:两网格解耦、边界谱文件可检查、不受重叠/掩码兼容性折磨。WW3Tool 只提供了 `ww3_multi` 这一条较难调通的路,没有 `ww3_bound` 方案。

### 3.6 网格对齐未保证
`ww3_multi` 插值在两网格**节点对齐**(细网格 dx 为粗网格 dx 的整数分之一、且起点经纬度重合)时最稳定、误差最小。WW3Tool 的 dx(0.05 vs 0.025)是 2:1(好),但**经纬度起点是否落在粗网格节点上没有约束**,可能产生半格错位、插值精度下降。

### 3.7 待核实项
- `inner_compute_precision` 是否真正写入了 `fine/` 的 `mod_def`,使细网格按自身 CFL 取更小步长;
- 共享谱在 `ww3_multi` 下是否对所有网格都一致写入(应一致,需确认未被某网格覆盖)。

## 4. 改进建议

### 4.1 首先明确"单向 vs 双向",按场景分流
- **单向(推荐为默认)**:小区域高分辨率、不需要反馈 → 走 `ww3_bound`/边界谱路线。粗网格(或全球再分析)产出 `nest.ww3`,细网格读取。两网格解耦,bathymetry/掩码可独立,最易调通。
- **双向**:确需细网格反馈粗网格(如近岸强非线性影响外海)→ 走 `ww3_multi`,但必须正确配置(见 4.3)。

### 4.2 inner 区域改由用户按 ROI 指定
把 inner 从"按系数机械收缩"改为**用户显式给定的感兴趣区域**(已在新 `grid.structured.nested.inner` 结构里支持),并校验 inner ⊂ outer、且边界离 outer 边界留足缓冲。

### 4.3 若保留 `ww3_multi` 双向路线,需补齐
1. **设置 `FLGHG1=T`**(粗网格在细网格下不重复计算)、按需设 `FLGHG2`;
2. **保证边界兼容**:细网格生成时,边界一圈强制取粗网格的水深/掩码(或在粗网格上预留细网格边界为湿点),避免边界落在陆地;
3. **`COMM_FRAC` 按计算量加权**:用 `points × (1/Δt)` 估算,而非纯点数;
4. **节点对齐**:让细网格起点经纬度对齐到粗网格节点,dx 取整数分之一(已满足 2:1);
5. **逐网格 CFL 时间步**:确认 `fine` 的 `mod_def` 用了更小的 DTXY/DTMAX。

### 4.4 验证手段
- 用 WW3 自带回归测试 `regtests/mww3_test_03`(多层 mosaic)对照参数;
- 跑一个小算例,检查 `ww3_multi` 日志是否报 "incompatible/missing boundary" 警告;
- 对比嵌套区与单网格高分辨率结果,确认边界处无能量突变(无伪梯度)。

## 5. 结论

WW3Tool 现用 `ww3_multi` 马赛克路线实现嵌套,但**最关键的双向嵌套开关 `FLGHG1/FLGHG2` 从未配置**,加上**两网格独立生成导致边界不兼容**、**`COMM_FRAC` 仅按点数分配忽略 CFL**、**inner 机械收缩**等问题,使当前嵌套既难保证正确性、也难保证效率。建议:**默认改用更稳健的单向嵌套(`ww3_bound`+边界谱)**;确需双向时,按 §4.3 正确配置 `FLGHG`、边界兼容、资源加权与节点对齐。
