# WW3Tool 多层(N 层)嵌套设计方案

目标:把当前**写死的两层(coarse/fine)**嵌套泛化为**任意 N 层**(嵌套套娃,逐层加密)。WW3 的 `ww3_multi` 原生支持多网格马赛克(回归测试 `mww3_test_03` 即 low0–3 + hgh0–3 多层),所以改动全部在 WW3Tool 侧。

## 1. 现状:两层硬编码在哪

| 组件 | 文件 | 硬编码点 |
| --- | --- | --- |
| 配置模型 | `config_models.py` | `GridConfig.outer` / `GridConfig.inner`(仅两个) |
| 配置解析 | `configuration.py` | `grid.structured.nested.{outer,inner}`;`_contract_region` 收缩出 inner |
| 网格生成 | `grid_generation_adapter.py` | `targets=[(coarse,outer),(fine,inner)]` 写死两目录 |
| nml 生成 | `ww3_multi_nml.py` | 读 `coarse/`、`fine/` 两个 ww3_grid.nml;COMM_FRAC 两网格切分 |
| nml 应用 | `modify_ww3_nml.py` | `_apply_all_params_nested` 写死 `coarse_dir`/`fine_dir` |
| 运行脚本 | `local.sh` / `server.sh` | 28/25 处 `coarse`/`fine`、文件搬运、`mpirun ww3_multi` |
| ww3_multi 模板 | `public/*/ww3_multi.nml` | `NRGRD=2`、`MODEL(1)=coarse`、`MODEL(2)=fine` |
| GUI | `grid_panel.py` | 固定"外网格参数 + 内网格参数"两块,`setup_outer_grid`/`setup_inner_grid` |

## 1.5 WW3 的硬性层数限制

- **`ww3_multi`(马赛克双向,本工具所用)**:网格总数 **`NRGRD ≤ 99`**(`w3nmlmultimd.F90:895` `MAX_NRGRD=99`,`MODEL(MAX_NRGRD)` 数组;`NRGRD>99` 时 `EXTCDE(9)` 中止)。纯套娃即最多 99 层,对实际嵌套(一般 2–5 层)绰绰有余。**实现时须校验 `NRGRD ≤ 99`**,超限直接报错,避免 WW3 崩。
- 单向嵌套(`ww3_shel`):一个网格最多向 **9 个**嵌套网格输出边界(手册 `app/nest.tex` "up to 9 files per model")——本工具不走此路,仅作对照。

## 2. 设计原则(WW3 侧约束)

- **套娃 / telescoping**:第 k+1 层必须完全落在第 k 层内部、分辨率更高;层间倍率(如 ×2 或 ×3)最好整数对齐、节点重合。
- **RANK**:`MODEL(i)%RESOURCE%RANK_ID = i`,**rank 越小越粗**(level0=最粗 rank 1,levelN=最细 rank N)。`ww3_multi` 按 rank 自动逐层交换数据。
- **谱一致**:所有层共享同一套 `SPECTRUM%`(频率/方向),否则 `ww3_multi` 报不兼容。
- **逐层 CFL 时间步**:每层 `DTXY ∝ Δx`,越细越小(已有 `_apply_cfl_timesteps_to_grid_nml`,天然支持 N 层)。
- **双向**:`FLGHG1=T`/`FLGHG2=T`(已开)。
- **COMM_FRAC**:N 个区间瓜分进程,按各层计算量 `点数/DTXY` 加权(已有两网格版,推广到 N)。

## 3. 各组件的 N 层改造

### 3.1 配置 schema(params.yml)
把 `outer/inner` 换成有序列表 `levels`(粗 → 细):
```yaml
grid:
  grid_type: nested
  lon: [110, 130]          # 最外层(level0)边界,缺省即 level0 的 lon/lat
  lat: [10, 31]
  structured:
    nested:
      levels:
        - { dx: 0.05,  dy: 0.05,  lon: [110,130], lat: [10,31] }   # level0 最粗
        - { dx: 0.025, dy: 0.025, lon: [114,126], lat: [14,27] }   # level1
        - { dx: 0.0125,dy: 0.0125,lon: [119,123], lat: [19,24] }   # level2 最细
```
- 模型:`GridConfig.nested_levels: List[GridRegion]`;为兼容现有消费方(grid_tools / region_renderer 用 `config.grid.outer`),令 `outer = levels[0]`、`inner = levels[-1]`。
- 解析:校验 `levels` 长度 ≥ 2、逐层 `dx` 递减、第 k+1 层 ⊂ 第 k 层。`nested_contraction_coefficient` 仅作"未显式给层时自动收缩"的默认。

### 3.2 网格目录命名
两层的 `coarse/`、`fine/` 改为**编号目录** `g0/ g1/ … gN/`(g0 最粗)。为兼容只跑过两层的旧案例,可保留"2 层时仍用 coarse/fine"的回退,但新逻辑一律按 `g0…gN`。

### 3.3 网格生成(grid_generation_adapter)
`_generate_structured` 的 `targets` 从固定两项改为**遍历 `levels`**:
```python
targets = [(workdir / f"g{i}", region) for i, region in enumerate(config.grid.nested_levels)]
```

### 3.4 ww3_multi.nml 生成(ww3_multi_nml.py)
- `DOMAIN%NRGRD = N`;
- 为每层生成 `MODEL(i)%NAME='gI'`、`RESOURCE = i 1 <frac_lo> <frac_hi> F`,`RANK_ID=i`;
- **COMM_FRAC 推广到 N**:`cost_i = 点数_i / DTXY_i`,`frac_i = cost_i / Σcost`,累积成区间 `[Σ_{<i}, Σ_{≤i}]`;
- 读各层 `gI/ww3_grid.nml` 的 NX/NY/DTXY(已有 `_read_grid_*` 助手)。

### 3.5 nml 应用(modify_ww3_nml)
`_apply_all_params_nested` 把"coarse 段 + fine 段"改成**对 `g0…gN` 循环**:每层 copy 模板 → sync grid.meta → 重算 CFL 时间步(`_apply_cfl_timesteps_to_grid_nml`)→ prnc/forcing/spectral。

### 3.6 运行脚本(local.sh / server.sh)
把写死的 coarse/fine 改成**发现并遍历 `g*/` 子目录**:
```bash
for g in $(ls -d g[0-9]* | sort -V); do
    ( cd "$g"; run_step "ww3_grid ($g)" ww3_grid; run_prnc_with_fields; run_step "ww3_strt ($g)" ww3_strt )
done
# 各层 mod_def/wind 搬运 → mod_def.gI …
mpirun -n $N ww3_multi
# 从最细层 gN 导出 ounp/ounf
```
文件搬运(`mod_def.coarse`/`.fine` → `mod_def.gI`)也按层循环。

### 3.7 GUI(grid_panel)
最大块的工作:把"外/内两块固定参数"改成**可增删的层列表**(➕ 加一层 / ➖ 删一层,每层一组 dx/lon/lat,地图上叠加显示各层框)。这部分我无法在此环境跑 PyQt 验证,需在 app 内测试。

## 4. 建议实施顺序

1. **配置层**:`GridConfig.nested_levels` + 解析/校验 + `outer/inner` 兼容别名(可在此环境验证 load)。
2. **生成 + nml + 脚本**(非 GUI 核心):生成 `g0…gN`、ww3_multi.nml 的 N-model + N-way COMM_FRAC、脚本循环。逻辑可单测。
3. **GUI**:层列表增删 + 地图叠加(需 app 内测试)。
4. 用 WW3 回归测试 `mww3_test_03` 的多层配置对照验证。

## 5. 需要你拍板的点

- **层数表示**:用 `levels` 列表(本方案),还是保留 `outer` + `inners: [...]`?
- **目录命名**:`g0…gN`,还是 `level0…`/`coarse,mid,fine`?
- **层定义方式**:每层都由用户显式给 `dx/lon/lat`,还是只给最细层 + 倍率,其余自动套娃?
- **范围**:先做"配置 + 生成 + nml + 脚本"非 GUI 核心(我能在此实现并验证逻辑),GUI 增删层后续单独做?还是要一并做?
